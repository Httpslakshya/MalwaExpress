# Project Rules for Malwa Express

- **Git Push Policy**: Do NOT automatically perform `git push` after code edits. Only make local changes. Execute `git push` ONLY when the user explicitly asks or commands to push updates to Git/Vercel.
